import './Banner.css'

const Banner = () => {
    //JSX -> Como o react lê o js e escreve no DOM
    return(
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner Principal da pag"></img>
        </header>  
    )
}

export default Banner